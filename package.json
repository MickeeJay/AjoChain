{
  "name": "ajochain",
  "private": true,
  "workspaces": [
    "contracts",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace packages/react-app",
    "tunnel": "npm run tunnel --workspace packages/react-app",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspace packages/react-app && npm run lint --workspace contracts",
    "test": "npm run test --workspace contracts",
    "clean": "npm run clean --workspace packages/react-app || true && npm run clean --workspace contracts || true"
  }
}